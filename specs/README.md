# specs/

**What to build.** Feature specs and requirements — the intended behavior before it exists.

One file per feature, kebab-case (e.g. `split-fairly.md`). A spec describes the goal, inputs/outputs, edge cases, and acceptance criteria. It is not implementation notes.
