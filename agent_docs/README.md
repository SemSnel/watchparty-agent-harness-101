# agent_docs/

**AI-only context.** Conventions, gotchas, code maps, and patterns that help an agent work in this repo correctly.

Terse and factual — written for an LLM, not a person. Examples: invariants ("money is always integer cents"), file-location maps, non-obvious wiring. Project-wide rules and commands stay in `AGENTS.md`; this is the deeper detail.
